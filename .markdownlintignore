node_modules/
vendor/
agent-os/
.agent-os/
# Local-only paths a developer may have in their checkout but that aren't
# tracked — the nested private docs/ repo (gitignored here) and draft dirs.
# CI doesn't see them; without this, the local Lefthook markdown_lint gate
# would fail on every push for anyone with these present.
docs/
working_files/
graphify-out/
