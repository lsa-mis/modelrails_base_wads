<%# #cb-scope wraps the bubble so the 0b axe audit scopes to the component, not the host chrome (dev theme toggle). %>
<div id="cb-scope" class="max-w-md">
  <%= ui :chat_bubble, sent: true do %>
    I'd like to book an appointment.
  <% end %>
</div>
