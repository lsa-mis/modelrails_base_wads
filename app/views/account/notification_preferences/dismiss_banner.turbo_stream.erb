<%# Replace the banner frame with an empty body so it vanishes in-place. -%>
<%= turbo_stream.replace "notifications_redesign_banner" do %>
  <%= turbo_frame_tag "notifications_redesign_banner" %>
<% end %>
