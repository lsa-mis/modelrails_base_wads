<% content_for(:title) { @resource.title } %>
<div class="max-w-4xl mx-auto px-4 py-16">
  <div class="flex items-start justify-between">
    <div>
      <h1 class="text-2xl font-bold text-slate-900 dark:text-gray-100">
        <%= @resource.title %>
      </h1>
      <div class="mt-2 flex items-center gap-3">
        <% if @resource.draft? %>
          <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium
                       bg-gray-100 dark:bg-gray-700 text-gray-700 dark:text-gray-300">
            <%= t("resources.index.draft") %>
          </span>
        <% else %>
          <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium
                       bg-green-100 dark:bg-green-900 text-green-800 dark:text-green-200">
            <%= t("resources.index.published") %>
          </span>
        <% end %>
        <span class="text-sm text-slate-500 dark:text-gray-400">
          <%= t("resources.show.created_by", name: @resource.created_by.full_name) %>
          &middot;
          <%= l @resource.created_at.to_date %>
        </span>
      </div>
    </div>
    <% if policy(@resource).update? %>
      <%= link_to t("resources.show.edit"),
            edit_workspace_project_resource_path(@workspace, @project, @resource),
            class: "min-h-[44px] flex items-center px-4 rounded-md
                    bg-sky-700 hover:bg-sky-800 text-white font-medium
                    focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-sky-700" %>
    <% end %>
  </div>

  <div class="mt-8 prose dark:prose-invert max-w-none">
    <%= render "workspaces/projects/resources/types/#{@resource.resourceable_type.underscore}", resource: @resource %>
  </div>

  <div class="mt-8">
    <%= link_to t("resources.show.back"),
          workspace_project_resources_path(@workspace, @project),
          class: "text-sky-700 dark:text-sky-400 hover:underline
                  focus:outline-none focus:ring-2 focus:ring-sky-700 rounded" %>
  </div>
</div>
