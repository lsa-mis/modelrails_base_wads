<%# locals: (title: nil, subtitle: nil, bg: "bg-white dark:bg-gray-900") %>
<section class="<%= bg %> py-16 sm:py-24">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <% if title %>
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold text-slate-900 dark:text-gray-100"><%= title %></h2>
        <% if subtitle %>
          <p class="mt-4 text-lg text-slate-600 dark:text-gray-400 max-w-2xl mx-auto"><%= subtitle %></p>
        <% end %>
      </div>
    <% end %>
    <%= yield %>
  </div>
</section>
